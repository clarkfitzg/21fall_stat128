# Outcomes
# 1. Develop Shiny apps by converting regular R code into Shiny code
# 2. Load external data using Shiny
# 3. Display plots in Shiny
# 4. Improve performance by reasoning through when code has to run, and placing it appropriately

# https://shiny.rstudio.com/tutorial/written-tutorial/lesson6/
# "Think of reactive expressions as links in a chain that connect input values to output objects.
# The objects in output will respond to changes made anywhere downstream in the chain. 
# (You can fashion a long chain because reactive expressions can call other reactive expressions.)